{
  "name": "@forge-freight/freight-mcp",
  "version": "0.1.0",
  "description": "Read-only MCP server exposing the Freight Core API to an agent.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^7.0.2",
    "vitest": "^3.2.7"
  }
}
